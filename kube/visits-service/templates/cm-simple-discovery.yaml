apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "visits-service.fullname" . }}-simple-discovery
  labels:
    {{- include "visits-service.labels" . | nindent 4 }}
data:
  application.yml: |
    spring:
      cloud:
        discovery:
          client:
            simple:
              instances:
                customers-service:
                  - host: http://customers-service.petclinic:8080/
                vets-service:
                  - host: http://vets-service.petclinic:8080/
