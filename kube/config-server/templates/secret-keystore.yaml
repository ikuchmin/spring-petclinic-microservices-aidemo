apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-secret-keystore
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  keystore.p12: {{ .Files.Get .Values.keystore.filePath | b64enc }}
