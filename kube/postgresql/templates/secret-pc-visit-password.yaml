apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-pc-visit-password
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.dbs.pc_customer.password | b64enc }}
