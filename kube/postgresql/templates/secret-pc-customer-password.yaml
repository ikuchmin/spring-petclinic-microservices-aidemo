apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql.fullname" . }}-pc-customer-password
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.dbs.pc_customers.password | b64enc }}
